# Shared Example Code

This module contains code that is shared across multiple game, demo, and test examples.

It includes common state management, utilities, and base classes to avoid duplication and maintain consistency across the showcase app.
